library(reticulate)
# See which Python reticulate is using
py_config()
# Install statsmodels into *that* environment
py_install("statsmodels", pip = TRUE)
# or if you prefer conda (only if this is a conda env):
# conda_install(envname = "r-reticulate", packages = "statsmodels")
reticulate::repl_python()
